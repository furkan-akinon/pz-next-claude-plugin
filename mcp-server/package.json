{
  "name": "@akinon/pz-next-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for pz-next plugin — dynamic npm registry queries for @akinon/pz-* packages",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
